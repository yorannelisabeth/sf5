{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="card-columns">
    {# //card-column   card-deck #}
        {% for l in livres %}
            {{include("livre/vignette.html.twig", {livre:l}) }}    

            {# autre facon de faire si on veut passer une nouvelle variable 
                 {% for l in livres %}
                {{include("livre/vignette.html.twig",{livre: l})}}  
            #}
            {% else %}
                  {{message|default("Il n'y a aucun livre dans la bibliothèque")}}
        {% endfor %}
    </div>

{% endblock %}
